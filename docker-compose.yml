version: '3'
services:
  dotnexus:
    build: 
      context: .
      dockerfile: DotNexus.App/Dockerfile
    ports:
     - "5001:5001"
  tritium:
    image: "tritium"
    ports:
     - "8080:8080"
     - "9090"
    privileged: true
    volumes:
      - ./tao-api:/root/.TAO