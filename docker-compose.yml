version: '3'
services:
  dotnexus:
    build: 
      context: .
      dockerfile: DotNexus.App/Dockerfile
    ports:
     - "5001:80"
  tritium:
    image: "tritium"
    hostname: "tritium"
    ports:
     - "8080"
     - "9090"
    privileged: true
    volumes:
      - ./tao-api:/root/.TAO